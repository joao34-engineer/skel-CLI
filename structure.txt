software-skeleton-cli/
├── bin/
│   └── skel.js              # The executable entry point (compiled)
├── src/
│   ├── commands/            # The "Verbs" of your tool
│   │   ├── init.ts          # Logic for "skel init"
│   │   ├── add.ts           # Logic for "skel add"
│   │   └── fuse.ts          # Logic for "skel fuse" (AI Logic)
│   ├── core/
│   │   ├── vault.ts         # Logic to fetch code from your DB/Repo
│   │   ├── ai-orchestrator.ts # Setup for the LLM (OpenAI/Anthropic)
│   │   └── file-system.ts   # Safe wrappers for fs-extra (writing files)
│   ├── types/
│   │   └── config.d.ts      # Type definitions for skeleton.config.json
│   └── index.ts             # Main Commander setup (The Router)
├── skeletons/               # (Temporary) Local folder to store your "Gold blocks"
│   ├── auth-jwt/
│   └── express-base/
├── package.json
└── tsconfig.json